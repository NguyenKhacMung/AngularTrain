import { AsyncForbidenNameDirective } from "./async-forbiden-name.directive";


describe('AsyncForbidenNameDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncForbidenNameDirective();
    expect(directive).toBeTruthy();
  });
});
